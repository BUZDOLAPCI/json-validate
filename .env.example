# json-validate MCP Server Configuration

# Transport type: 'stdio' or 'http'
TRANSPORT=stdio

# HTTP server port (only used when TRANSPORT=http)
PORT=3000

# HTTP server host (only used when TRANSPORT=http)
HOST=127.0.0.1

# Log level: 'debug', 'info', 'warn', 'error'
LOG_LEVEL=info
